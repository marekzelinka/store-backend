## TODO

- handle delete cascade
- add pagination to GET /products, GET /products/categories/1 GET /categories, GET /categories/1/products
- add `is_active` param to GET /categories, GET /products, GET /products/
- add GET /products/active, GET /categories/active
- add GET /categories/1/products with `is_active` and `limit` & `offset` pagination
